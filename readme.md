run
python```
pip install -r requirements.txt```

or

python```
pip install pygame torch pandas numpy scikit-learn qrcode aiohttp werkzeug mediapipe opencv-python
```


python -m pip install cryptography


currently using a get_local_ip() function . However, the reason it might fail on your PCs is often the Windows Defender Firewall. When they run the server for the first time, Windows will ask for permission. You must click "Allow on Private Networks.

